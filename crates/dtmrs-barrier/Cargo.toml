[package]
name = "dtmrs-barrier"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "子事务屏障：一张表同时解决幂等、空回滚、悬挂"

[dependencies]
dtmrs-core = { path = "../dtmrs-core" }
sqlx = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
